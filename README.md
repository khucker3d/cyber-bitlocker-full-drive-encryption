# cyber-bitlocker-full-drive-encryption
BitLocker is Windows full disk encryption. Once enabled, Windows encrypts the drive and requires trusted boot (TPM) or a key to unlock it. This protects data at rest, even if someone boots from external media or pulls the drive.
